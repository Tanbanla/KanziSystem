03/03/2026
k có mã nhà cung cấp, cũng đăng xin báo giá dc
- thêm mã đơn yêu cầu vào file nhập báo giá nhà cung cấp

06/03/2026
-- dong đầu tiên k nhảy chủng loại hàng
- nhảy trùng nhà cung cấp (Không phải trung mà là 2 thằng 2 PIC)

-- kiểm tra tất cả rồi mới out lỗi, thời gian gian lỗi vẫn cho up, blane vẫn cho up thông tin 
-- lưu lịch sử khi nhập bao giá 

03/09/2026
- màn hình nhập upload dữ liệu ncc
-- màn hình xác nhận tên, gom theo mà linh kiện nhà cung, thêm phê duyệt nhiều
-- mail chỉnh sửa cho gửi , 
- cho phép sửa đến hết khi đẩy đi 
-- tạo lựa chọn trong file báo giá , accept, lỗi lấy thông tin gửi nhà cung cấp 
- xử lý trường hợp nhà cung cấp từ chối báo giá.
13/03/2026
-- them download file excel man hinh phe duyet bao gia
-- file gui nha cung cap bi lech cot
-- them cac coo ma hang NCC, ma thiet bi
-- trang thais don
-- trang thai, dang cho xac nhan de an, 
-- font do, mau check lai, dieu kien hien thi mau man hinh thong tin nha cung cap 
-- tinh tong tien don hang theo nha cung cap
16/03/2026
-- lỗi giờ server màn hình tạo yêu cầu báo giá 
-- chữ hiển thị đang xuất phải là đang nhập, thêm loading cho xuất file templea
-- Phê duyệt yêu cầu báo giá(chỉnh lại tên)
-- thời gian bị lùi màn hình phê duyệt
-- màn xác nhận tên , cho xuất all dữ liệu, nhập check lại code ,
xuất mã mới theo tên bị trùng.
- sau khi up đủ thông tin thêm điều kiện check mà hàng ncc tương ứng vs mã hàng nội bộ 
- Hàng ngoài danh mục, mới tính.

-- Lưu tất cả,
-- check lỗi nhập bị trùng đơn hoặc trong master, mã hàng đã có NcC
-- thêm chức năng trả lại các đơn tạo mã lỗi gửi lại cho pur xử lý
-- sửa lại tiêu đề tên mail gửi NCC
- chỉnh lại tỷ lệ màn hình
- khi lưu thì gửi mail
18/03/2026
-- mail trả lại gửi lại pic 
-- k lấy được dc báo giá thì trả cho phòng ban mail 4
- trả lại hiển thị ở màn hình lựa chọn nhà cung cấp, trạng thái từ chối phê duyệt
-- Khi ship và ACC nhấn lưu thì link kết quả sang đơn tương ứng
- thêm chức năng cho họ gửi sang màn hình báo giá phòng ban
-- file màn hình xác nhận tên, ẩn cột với các user quyền khác nhau
-- giữ tiêu đề column màn hình lịch sử
-- xóa các mục hàng ở trên file nhập báo giá
- thông tin đã sửa thì hiển thị phần màu xanh các mục đã sửa
- thêm xuất dữ liệu đã phê duyệt ở màn hình xác nhận tên 
- đã nhận mới cho lên đầu, thêm hiển thị cho họ biết mã đến stt bao nhiêu
- Xác nhận vụ tạo mã hàng nội bộ, trên hệ thống 
-- Màn hình phê duyệt thêm stt
-- sửa lại các file xuất, thêm người làm yêu cầu
-- Màn hình phê duyệt chi tiết, thêm các cột bị thiếu







